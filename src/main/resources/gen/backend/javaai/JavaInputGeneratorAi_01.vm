Here is class $!{domainClassSimple}:

```java
$!{domainClassStr}
```java

Using class $!{domainClassSimple} create a new class. For class name add suffix `Input`.
For every field of type that is not defined in ('java.lang', 'java.time') packages add suffix `Input` to type name.
For every field of type that is not defined in ('java.lang', 'java.time') packages change type to 'String'.
For every field that is primitive type change type to 'String'.
Do not add suffix `Input` to fields of type defined in ('java.lang', 'java.time')  packages.
Make all class fields private.


